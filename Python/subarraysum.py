def subarraySum(arr):
    maxLength = len(arr)
    numOfArrays = range(maxLength, 1)
    inc = 0
    for num in arr:
        inc += num
    for num in numOfArrays:
        sideArray = []

    # while not maxLength == 0:


subarraySum([1, 2, 3])
